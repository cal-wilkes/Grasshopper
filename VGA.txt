library ieee;
use ieee.std_logic_1164.all;
use ieee.std_logic_arith.all;
use ieee.std_logic_unsigned.all;


entity vga is
	port( clk: in std_logic;
			reset: in std_logic;
			);
end vga;

architecture Behavioral of vga is

type int_array is array (0 to 2) of integer;
signal distance : int_array;

--scan_line_x: in std_logic_vector(10 downto 0);
--scan_line_y: in std_logic_vector(10 downto 0);
--box_width: buffer std_logic_vector(8 downto 0);
--box_color: in std_logic_vector(11 downto 0);
--kHz: in std_logic;
--red: out std_logic_vector(3 downto 0);
--blue: out std_logic_vecotr(3 downto 0);
--green: out std_logic_vector(3 downto 0)
--constant box_loc_x_min: std_logic_vector(9 downto 0) := "0000000000";
--constant box_loc_y_min: std_logic_vector(9 downto 0) := "0000000000";
--signal box_loc_x_max: std_logic_vector(9 downto 0);
--signal box_loc_y_max: std_logic_vector(9 downto 0);
--signal pixel_color: std_logic_vector(11 downto 0) := "000000000000"; -- i think represents black
--signal box_loc_x, box_loc_y: std_logic_vector(9 downto 0);



-- have 3 defined postions
	-- 1st number takes 3 widths in x
	-- space
	-- second number takes 3 widths in x
	-- space
	-- decimal point takes 1 width in x
	-- third number takes 3 widths in x
	-- followed by units later on

-- determining the numbers and the places they'll be in will depend on distance(2), distance(1), and distance(0)
-- could essentially repeat the following below 3 times with the necessary offsets



lookup table: process(clk)
begin

	if(box_width > "000100000") then
		box_width <= "000100000";
	end if;
	
	elsif(placeholder_name = 0) then ---will change the logic later, need to think of the best way to do it
		pixel_color <= box_color when ((((scan_line_x >= box_loc_x) and (scan_line_x < box_loc_x + box_width + box_width + box_width)) and -- top of the zero
										((scan_line_y >= box_loc_y) and (scan_line_y < box_loc_y + box_width)))
										or
										(((scan_line_x = box_loc_x) and (scan_line_x = box_loc_x + box_width + box_width)) and -- second, third and fourth level of the zero
										((scan_line_y = box_loc_y + box_width) and (scan_line_y < box_loc_y + box_width + box_width + box_width)))
										or
										(((scan_line_x >= box_loc_x) and (scan_line_x < box_loc_x + box_width + box_width)) and -- top of the zero
										((scan_line_y = box_loc_y + box_width + box_width + box_width + box_width) and (scan_line_y < box_loc_y + box_width + box_width + box_width + box_width + box_width))))
										
	elsif(placeholder_name = 1) then
		pixel_color <= box_color when ((scan_line_x =box_loc_x) and
										((scan_line_y >= box_loc_y) and (scan_line_y < box_loc_y + box_width + box_width + box_width + box_width + box_width)))
										
	elsif(placeholder_name = 2) then
		pixel_color <= box_color when ((((scan_line_x >= box_loc_x) and (scan_line_x < box_loc_x + box_width + box_width + box_width)) and -- top level of 2
										((scan_line_x >= box_loc_y) and (scan_line_y < box_loc_y + box_width)))
										or
										((scan_line_x = box_loc_x + box_width + box_width + box_width + box_width) and -- second level of 2
										(scan_line_y = box_loc_y + box_width))
										or
										(((scan_line_x >= box_loc_x) and (scan_line_x < box_loc_x + box_width + box_width + box_width)) and -- third level of 2
										((scan_line_x = box_loc_y + box_width + box_width) and (scan_line_y < box_loc_y + box_width + box_width + box_width)))
										or
										((scan_line_x = box_loc_x) and -- fourth level of 2
										(scan_line_y = box_loc_y + box_width + box_width + box_width) and (scan_line_y < box_loc_y + box_width + box_width))
										or
										(((scan_line_x >= box_loc_x) and (scan_line_x < box_loc_x + box_width + box_width + box_width)) and -- bottom level of 2
										((scan_line_x = box_loc_y + box_width + box_width + box_width + box_width) and (scan_line_y < box_loc_y + box_width + box_width + box_width + box_width + box_width))))
										
	elsif(placeholder_name = 3) then
		pixel_color <= box_color when ((((scan_line_x >= box_loc_x) and (scan_line_x < box_loc_x + box_width + box_width + box_width)) and -- top level of 3
										((scan_line_x >= box_loc_y) and (scan_line_y < box_loc_y + box_width)))
										or
										((scan_line_x = box_loc_x + box_width + box_width) and -- second level of 3
										(scan_line_y = box_loc_y + box_width))
										or
										(((scan_line_x >= box_loc_x) and (scan_line_x < box_loc_x + box_width + box_width + box_width)) and -- third level of 3
										((scan_line_x = box_loc_y + box_width + box_width) and (scan_line_y < box_loc_y + box_width + box_width + box_width)))
										or
										((scan_line_x = box_loc_x + box_width + box_width) and -- fourth level of 3
										(scan_line_y = box_loc_y + box_width + box_width + box_width) and (scan_line_y < box_loc_y + box_width + box_width + box_width + box_width))
										or
										(((scan_line_x >= box_loc_x) and (scan_line_x < box_loc_x + box_width + box_width + box_width)) and -- bottom level of 3
										((scan_line_x = box_loc_y + box_width + box_width + box_width + box_width) and (scan_line_y < box_loc_y + box_width + box_width + box_width + box_width + box_width))))
										
	elsif(placeholder_name = 4) then
		pixel_color <= box_color when (((scan_line_x = box_loc_x) or (scan_line_x = box_loc_x + box_width + box_width)) and -- top and second level of 4 -- double check
										((scan_line_y >= box_loc_y) and (scan_line_y < box_loc_y + box_width + box_width)))
										or
										(((scan_line_x >= box_loc_x) and (scan_line_x < box_loc_x + box_width + box_width + box_width)) and -- third level of 4
										((scan_line_y = box_loc_y + box_width + box_width) and (scan_line_y < box_loc_y + box_width + box_width + box_width)))
										or
										((scan_line_x = box_loc_x + box_width + box_width) and -- fourth and fifth level of 4
										((scan_line_y >= box_loc_y + box_width + box_width + box_width) and (scan_line_y < box_loc_y + box_width + box_width + box_width + box_width))) 
								
	elsif(placeholder_name = 5) then
		pixel_color <= box_color when ((((scan_line_x >= box_loc_x) and (scan_line_x < box_loc_x + box_width + box_width + box_width)) and -- top level of 5
										((scan_line_x >= box_loc_y) and (scan_line_y < box_loc_y + box_width)))
										or
										((scan_line_x = box_loc_x) and -- second level of 5
										(scan_line_y = box_loc_y + box_width))
										or
										(((scan_line_x >= box_loc_x) and (scan_line_x < box_loc_x + box_width + box_width + box_width)) and -- third level of 5
										((scan_line_x = box_loc_y + box_width + box_width) and (scan_line_y < box_loc_y + box_width + box_width + box_width)))
										or
										((scan_line_x = box_loc_x + box_width + box_width + box_width + box_width) and -- fourth level of 5
										(scan_line_y = box_loc_y + box_width + box_width + box_width) and (scan_line_y < box_loc_y + box_width + box_width))
										or
										(((scan_line_x >= box_loc_x) and (scan_line_x < box_loc_x + box_width + box_width + box_width)) and -- bottom level of 5
										((scan_line_x = box_loc_y + box_width + box_width + box_width + box_width) and (scan_line_y < box_loc_y + box_width + box_width + box_width + box_width + box_width))))


	elsif(placeholder_name = 6) then
		pixel_color <= box_color when ((scan_line_x = box_loc_x) and -- top and second level of 6
										((scan_line_y >= box_loc_y) and (scan_line_y < box_loc_y + box_width + box_width)))
										or
										(((scan_line_x >= box_loc_x) and (scan_line_x < box_loc_x + box_width + box_width + box_width)) and --third level of 6
										((scan_line_y = box_loc_y + box_width + box_width) and (scan_line_y < box_loc_y + box_width + box_width + box_width)))
										or
										(((scan_line_x = box_loc_x) or (scan_line_x = box_loc_x + box_width + box_width)) and -- fourth level of 6
										((scan_line_y = box_loc_y + box_width + box_width + box_width) and (scan_line_y < box_loc_y + box_width + box_width + box_width)))

	elsif(placeholder_name = 7) then
		pixel_color <= box_color when ((((scan_line_x >= box_loc_x) and (scan_line_x < box_loc_x + box_width + box_width + box_width)) and -- top level of 7
										((scan_line_y = box_loc_y) and (scan_line_x < box_loc_y + box_width)))
										or
										((scan_line_x = box_loc_x) and -- rest of 7
										((scan_line_y >= box_loc_y) and (scan_line_y < box_loc_y + box_width + box_width + box_width + box_width + box_width))))
									
	elsif(placeholder_name = 8) then ---will change the logic later, need to think of the best way to do it
		pixel_color <= box_color when ((((scan_line_x >= box_loc_x) and (scan_line_x < box_loc_x + box_width + box_width + box_width)) and -- top of 8
										((scan_line_y >= box_loc_y) and (scan_line_y < box_loc_y + box_width)))
										or
										(((scan_line_x = box_loc_x) and (scan_line_x = box_loc_x + box_width + box_width)) and -- second and fourth level of the 8
										((scan_line_y = box_loc_y + box_width) or (scan_line_y = box_loc_y + box_width + box_width)))
										or
										(((scan_line_x >= box_loc_x) and (scan_line_x < box_loc_x + box_width + box_width + box_width)) and -- third level of 8
										((scan_line_y = box_loc_y + box_width + box_width) and (scan_line_y < box_loc_y + box_width + box_width + box_width)))
										or
										(((scan_line_x >= box_loc_x) and (scan_line_x < box_loc_x + box_width + box_width)) and -- top of the 8
										((scan_line_y = box_loc_y + box_width + box_width + box_width + box_width) and (scan_line_y < box_loc_y + box_width + box_width + box_width + box_width + box_width))))
								
	elsif(placeholder_name = 9) then
		pixel_color <= box_color when (((scan_line_x >= box_loc_x) or (scan_line_x < box_loc_x + box_width + box_width)) and -- top level of 9 -- double check
										((scan_line_y >= box_loc_y) and (scan_line_y < box_loc_y + box_width + box_width)))
										or
										(((scan_line_x = box_loc_x) or (scan_line_x = box_loc_x + box_width + box_width)) and -- second level of 9 -- double check
										((scan_line_y >= box_loc_y) and (scan_line_y < box_loc_y + box_width + box_width)))
										or
										(((scan_line_x >= box_loc_x) and (scan_line_x < box_loc_x + box_width + box_width + box_width)) and -- third level of 9
										((scan_line_y = box_loc_y + box_width + box_width) and (scan_line_y < box_loc_y + box_width + box_width + box_width)))
										or
										((scan_line_x = box_loc_x + box_width + box_width) and -- fourth and fifth level of 9
										((scan_line_y >= box_loc_y + box_width + box_width + box_width) and (scan_line_y < box_loc_y + box_width + box_width + box_width + box_width))) 
										
										
										
										
										
										
										
										
										
										
										
										
										
										
										